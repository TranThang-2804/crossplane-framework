apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crossplane-components.fullname" . }}-test-connection"
  labels:
    {{- include "crossplane-components.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crossplane-components.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
